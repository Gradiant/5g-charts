# enable scp function:
scp:
  enabled: true

# set scp configuration in the other functions:
smf:
  config:
    sbi:
      client:
        scp:
          enabled: true
amf:
  config:
    sbi:
      client:
        scp:
          enabled: true
ausf:
  config:
    sbi:
      client:
        scp:
          enabled: true
bsf:
  config:
    sbi:
      client:
        scp:
          enabled: true
nssf:
  config:
    sbi:
      client:
        scp:
          enabled: true
pcf:
  config:
    sbi:
      client:
        scp:
          enabled: true
udm:
  config:
    sbi:
      client:
        scp:
          enabled: true
udr:
  config:
    sbi:
      client:
        scp:
          enabled: true
