# enable scp function:
scp:
  enabled: true

# set scp configuration in the other fuctions:
smf:
  config:
    scp:
      enabled: true
amf:
  config:
    scp:
      enabled: true
ausf:
  config:
    scp:
      enabled: true
bsf:
  config:
    scp:
      enabled: true
nrf:
  config:
    scp:
      enabled: true
nssf:
  config:
    scp:
      enabled: true
pcf:
  config:
    scp:
      enabled: true
udm:
  config:
    scp:
      enabled: true
udr:
  config:
    scp:
      enabled: true
