apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}-icscf-frdi
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
  {{- if .Values.kamailio.icscf.services.frdi.annotations }}
  annotations:
    {{- toYaml .Values.kamailio.icscf.services.frdi.annotations | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.kamailio.icscf.services.frdi.type }}
  {{- if or (eq .Values.kamailio.icscf.services.frdi.type "LoadBalancer") (eq .Values.kamailio.icscf.services.frdi.type "NodePort") }}
  externalTrafficPolicy: Cluster
  {{- end }}
  ports:
    - port: {{ .Values.kamailio.icscf.services.frdi.port }}
      targetPort: icscf-frdi-udp
      protocol: UDP
      name: icscf-frdi-udp
    - port: {{ .Values.kamailio.icscf.services.frdi.port }}
      targetPort: icscf-frdi-tcp
      protocol: TCP
      name: icscf-frdi-tcp
  publishNotReadyAddresses: true
  selector: {{- include "common.labels.matchLabels" . | nindent 4 }}