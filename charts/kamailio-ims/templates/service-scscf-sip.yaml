apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}-scscf-sip
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
  {{- if .Values.kamailio.scscf.services.sip.annotations }}
  annotations:
    {{- toYaml .Values.kamailio.scscf.services.sip.annotations | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.kamailio.scscf.services.sip.type }}
  {{- if or (eq .Values.kamailio.scscf.services.sip.type "LoadBalancer") (eq .Values.kamailio.scscf.services.sip.type "NodePort") }}
  externalTrafficPolicy: Cluster
  {{- end }}
  ports:
    - port: {{ .Values.kamailio.scscf.services.sip.port }}
      targetPort: scscf-sip-udp
      protocol: UDP
      name: scscf-sip-udp
    - port: {{ .Values.kamailio.scscf.services.sip.port }}
      targetPort: scscf-sip-tcp
      protocol: TCP
      name: scscf-sip-tcp
  publishNotReadyAddresses: true
  selector: {{- include "common.labels.matchLabels" . | nindent 4 }}