apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" . }}-fhoss-entrypoint
  labels: {{- include "common.labels.standard" . | nindent 4 }}
data:
  fhoss-k8s-entrypoint.sh: |
{{ tpl (.Files.Get "resources/fhoss/k8s-entrypoint.sh") . | indent 4 }}