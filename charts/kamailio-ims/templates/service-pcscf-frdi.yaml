apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}-frdi
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
  {{- if .Values.kamailio.pcscf.services.frdi.annotations }}
  annotations:
    {{- toYaml .Values.kamailio.pcscf.services.frdi.annotations | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.kamailio.pcscf.services.frdi.type }}
  externalTrafficPolicy: Cluster
  ports:
    - port: {{ .Values.kamailio.pcscf.services.frdi.port }}
      targetPort: pcscf-frdi-udp
      protocol: UDP
      name: pcscf-frdi-udp
    - port: {{ .Values.kamailio.pcscf.services.frdi.port }}
      targetPort: pcscf-frdi-tcp
      protocol: TCP
      name: pcscf-frdi-tcp
  publishNotReadyAddresses: true
  selector: {{- include "common.labels.matchLabels" . | nindent 4 }}