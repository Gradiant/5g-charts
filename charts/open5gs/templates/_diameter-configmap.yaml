{{- define "open5gs.diameter-configmap" -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" . }}-{{ .component }}-diameter
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: {{ .component }}
data:
  {{ .component }}.conf: |-
{{ tpl (.Files.Get (printf "resources/config/diameter-%s.conf" .component)) . | indent 4 }}
{{- end -}}