apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" . }}-uerouting
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: smf
data:
  uerouting.yaml: |
{{ tpl (.Files.Get "resources/uerouting.yaml") . | indent 4 }}
