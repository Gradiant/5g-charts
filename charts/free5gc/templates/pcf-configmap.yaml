apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" . }}-pcf
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: pcf
data:
  pcfcfg.yaml: |
{{ tpl (.Files.Get "resources/pcfcfg.yaml") . | indent 4 }}
