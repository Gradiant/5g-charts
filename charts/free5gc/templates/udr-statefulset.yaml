apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "common.names.fullname" . }}-udr
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: udr
spec:
  replicas: 1
  selector:
    matchLabels: {{ include "common.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: udr
  serviceName: {{ template "common.names.fullname" . }}-udr
  template:
    metadata:
      labels: {{- include "common.labels.standard" . | nindent 8 }}
        app.kubernetes.io/component: udr
    spec:
{{ include "free5gc.imagePullSecrets" . | nindent 6 }}
      initContainers:
      - name: wait-nrf
        image: busybox:1.28
        env:
        - name: DEPENDENCIES
          value: http://{{ include "common.names.fullname" . }}-nrf
        command: ['sh', '-c', 'set -x; sleep 45;']
      containers:
      - name: udr
        image: {{ template "free5gc.image" . }}
        imagePullPolicy: {{ .Values.image.pullPolicy | quote  }}
        command: ["udr", "-udrcfg", "/etc/free5gc/udrcfg.yaml"]
        ports:
        - name: http
          containerPort: 8000
        volumeMounts:
        - name: udr-config
          mountPath: /etc/free5gc/udrcfg.yaml
          subPath: "udrcfg.yaml"
      volumes:
      - name: udr-config
        configMap:
          name: {{ include "common.names.fullname" . }}-udr
