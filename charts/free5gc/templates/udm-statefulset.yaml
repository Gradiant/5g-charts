apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "common.names.fullname" . }}-udm
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: udm
spec:
  replicas: 1
  selector:
    matchLabels: {{ include "common.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: udm
  serviceName: {{ template "common.names.fullname" . }}-udm
  template:
    metadata:
      labels: {{- include "common.labels.standard" . | nindent 8 }}
        app.kubernetes.io/component: udm
    spec:
{{ include "free5gc.imagePullSecrets" . | nindent 6 }}
      initContainers:
      - name: wait-nrf
        image: busybox:1.28
        env:
        - name: DEPENDENCIES
          value: http://{{ include "common.names.fullname" . }}-nrf
        command: ['sh', '-c', 'set -x; sleep 45;']
      containers:
      - name: udm
        image: {{ template "free5gc.image" . }}
        imagePullPolicy: {{ .Values.image.pullPolicy | quote  }}
        command: ["udm", "-udmcfg", "/etc/free5gc/udmcfg.yaml"]
        ports:
        - name: http
          containerPort: 8000
        volumeMounts:
        - name: udm-config
          mountPath: /etc/free5gc/udmcfg.yaml
          subPath: "udmcfg.yaml"
      volumes:
      - name: udm-config
        configMap:
          name: {{ include "common.names.fullname" . }}-udm
