apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "common.names.fullname" . }}-ausf
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: ausf
spec:
  replicas: 1
  selector:
    matchLabels: {{ include "common.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: ausf
  serviceName: {{ template "common.names.fullname" . }}-ausf
  template:
    metadata:
      labels: {{- include "common.labels.standard" . | nindent 8 }}
        app.kubernetes.io/component: ausf
    spec:
{{ include "free5gc.imagePullSecrets" . | nindent 6 }}
      initContainers:
      - name: wait-nrf
        image: busybox:1.28
        env:
        - name: DEPENDENCIES
          value: http://{{ include "common.names.fullname" . }}-nrf
        command: ['sh', '-c', 'set -x; sleep 45;']
      containers:
      - name: ausf
        image: {{ template "free5gc.image" . }}
        imagePullPolicy: {{ .Values.image.pullPolicy | quote  }}
        command: ["ausf", "-ausfcfg", "/etc/free5gc/ausfcfg.yaml"] 
        ports:
        - name: http
          containerPort: 8000
        volumeMounts:
        - name: ausf-config
          mountPath: /etc/free5gc/ausfcfg.yaml
          subPath: "ausfcfg.yaml"
      volumes:
      - name: ausf-config
        configMap:
          name: {{ include "common.names.fullname" . }}-ausf
