apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" . }}-upf
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: upf
data:
  upfcfg.yaml: |
{{ tpl (.Files.Get "resources/upfcfgb.yaml") . | indent 4 }}
