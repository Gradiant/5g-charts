apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" . }}-nrf
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: nrf
data:
  nrfcfg.yaml: |
{{ tpl (.Files.Get "resources/nrfcfg.yaml") . | indent 4 }}
