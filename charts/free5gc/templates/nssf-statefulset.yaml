apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "common.names.fullname" . }}-nssf
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: nssf
spec:
  replicas: 1
  selector:
    matchLabels: {{ include "common.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: nssf
  serviceName: {{ template "common.names.fullname" . }}-nssf
  template:
    metadata:
      labels: {{- include "common.labels.standard" . | nindent 8 }}
        app.kubernetes.io/component: nssf
    spec:
{{ include "free5gc.imagePullSecrets" . | nindent 6 }}
      initContainers:
      - name: wait-nrf
        image: busybox:1.28
        env:
        - name: DEPENDENCIES
          value: http://{{ include "common.names.fullname" . }}-nrf
        command: ['sh', '-c', 'set -x; sleep 45;']
      containers:
      - name: nssf
        image: {{ template "free5gc.image" . }}
        imagePullPolicy: {{ .Values.image.pullPolicy | quote  }}
        command: ["nssf", "-nssfcfg", "/etc/free5gc/nssfcfg.yaml"]
        ports:
        - name: http
          containerPort: 8000
        volumeMounts:
        - name: nssf-config
          mountPath: /etc/free5gc/nssfcfg.yaml
          subPath: "nssfcfg.yaml"
      volumes:
      - name: nssf-config
        configMap:
          name: {{ include "common.names.fullname" . }}-nssf
