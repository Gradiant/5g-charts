info:
  version: 1.0.0
  description: UPF configuration

configuration:
  #debugLevel: panic|fatal|error|warn|info|debug|trace
  debugLevel: info

  pfcp:
    - addr: {{ include "common.names.fullname" . }}-upf

  gtpu:
    - addr: {{ include "common.names.fullname" . }}-upf
    # [optional] gtpu.name
    # - name: "{{ include "common.names.fullname" . }}-upf"
    # [optional] gtpu.ifname
    # - ifname: gtpif

  dnn_list:
    - dnn: internet
      cidr: {{ .Values.global.ueSubnet }}
      # [optional] apn_list[*].natifname
      # natifname: eth0
    #- dnn: internet2
      #cidr: 10.10.0.0/16
      # [optional] apn_list[*].natifname
      # natifname: eth0
